// Ingestion entrypoint. Provider client, normalizers, diffing, and push
// notifications land here in later phases (see docs/provider-decision.md).
console.log("hooply worker: no ingestion configured yet");
